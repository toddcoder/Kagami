data Expr {
	Number(Int)
	Sum(Expr, Expr)
}

Sum(Number(1), Number(2))

func eval(e) {
	let result = match e {
		case Number(n) = n
		case Sum(e1, e2) = eval(e1) + eval(e2)
	}
	return result
}

eval(Sum(Number(1), Number(2)))