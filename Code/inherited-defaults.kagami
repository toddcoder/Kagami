open sys

#pragma ALLOW_PRINT_STATEMENT true

class Coffee(name, strength = 1) {
	fn brew() => println $"brewing $name"
	fn pour(amount = 1) {
		if amount == 1 {
			return "Poured a single cup"
		}
		else {
			return $"Poured $amount cups"
		}
	}
}

class MaxgoodHouse(name, strength = 0) is Coffee(name, strength) {
	let brand = "Maxgood House"

	override fn brew() => println $"Brewing $brand $name"
	override fn pour(amount = 0) => $"(super.pour(amount)), but it sucks"
}

let boring = MaxgoodHouse("Boring")
boring.brew()
println boring.pour()