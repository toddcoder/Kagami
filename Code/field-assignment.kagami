open sys

fn sum_square(f, m, n) {
	var sum = 0.0
	for k in m to n sum += f(k) ^ 2
	return sum
}

println($" First computation: (sum_square(x -> 1f / x, 1, 10))")
println($"Second computation: (sum_square(x -> math.sin(x), 2, 13))")