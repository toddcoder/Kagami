open sys

class Logger(var name string) {
	var mute bool
	fn log(msg string) {
		if not mute {
			println(msg)
		}
	}
}
static {
	var cache = {:}.memo() name -> Logger(name)
	fn [](name) => cache[name]
}
var logger = Logger["UI"]
logger.log("Button clicked")