open sys

pattern Odd(o) => o !% 2 ?? o
pattern Even(e) => e %% 2 ?? e
pattern Zero(z) => z == 0 ?? z

for i in [3, 2, 0] {
	match i {
		| Zero(z) => println($"Zero=(z)")
		| Odd(o) => println($"Odd=(o)")
		| Even(e) => println($"Even=(e)")
	}
}