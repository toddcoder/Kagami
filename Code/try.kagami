open sys

#pragma ALLOW_PRINT_STATEMENT true

fn show(name string) string {
	reject name.isEmpty throw f"Name cannot be empty"
	return name.upper()
}

let notEmpty = try show("foo")
if notEmpty {
	println notEmpty
}

let empty = try show("")
if not empty {
	println empty
}