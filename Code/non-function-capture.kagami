open sys

enum Expr {
	| Var(name)
	| Numeric(number)
	| UnOp(op, args)
	| BinOp(op, left, right)
}

func simplifyTop()
	| UnOp("-", UnOp("-", e)) return e
	| BinOp("+", e, Numeric(0)) return e
	| BinOp("*", e, Numeric(1)) return e
	| e return e

simplifyTop(UnOp("-", UnOp("-", Numeric(42))))