#pragma ALLOW_PRINT_STATEMENT true

builder rounding(sigDig int, x1 float, x2 float, y1 int, y2 int) {
	let x = x1 / x2
	let y = y1 / y2
	return math.round(x + y, sigDig)
}

let r1 = rounding(3, math.pi, 2.0, 20, 1)
println r1

let r2 = rounding(3, math.pi, 2.0, 20, 0)
println r2