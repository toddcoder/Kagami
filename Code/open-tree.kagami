open sys

enum Tree {
	| Nilt
	| Node(n, t1, t2)
}

let t1 = Node(7, Node(3, Nilt, Nilt), Node(4, Nilt, Nilt))

match mirror()
	| Nilt return Nilt
	| Node(a, x, y) return Node(a, mirror(y), mirror(x))
println(mirror(t1))