open sys

enum Tree {
	when Nilt
	when Node(n, t1, t2)
}

open Tree
let t1 = node(7, node(3, nilt, nilt), node(4, nilt, nilt))

func mirror()
	when Nilt return nilt
	when Node(a, x, y) return node(a, mirror(y), mirror(x))
println(mirror(t1))