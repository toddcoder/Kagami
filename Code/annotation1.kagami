open sys

#pragma ALLOW_PRINT_STATEMENT true

annotation Record(selector Selector, f lambda) {
	\names << selector.name
}
static {
	let names = {}
}

@Record
fn alfa(x) => x * 2

@Record
fn bravo(x) => x / 10

@Record
fn charlie(x) => x * 7

println Record.names